import { Input } from "@/components/common/Input";
const Home: React.FC = () => {
  return (
    <div>
      Home
      <Input value="" onChange={() => {}} />
    </div>
  );
};

export default Home;

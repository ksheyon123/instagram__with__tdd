import { LoginForm } from "@/components/Login/LoginForm";

const Home: React.FC = () => {
  const onClick = () => {};
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Home;

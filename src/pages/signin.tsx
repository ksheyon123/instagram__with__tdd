import { LoginForm } from "@/components/Login/LoginForm";

const SignIn: React.FC = () => {
  const onClick = () => {};
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default SignIn;

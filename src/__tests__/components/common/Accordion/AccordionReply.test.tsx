import { fireEvent, render } from "@testing-library/react";
import "@testing-library/jest-dom";

// Test Cases : 댓글 텍스트, 작성 버튼, 하위 Item 열람

describe("Accordion Reply Component", () => {
  it("", () => {});
});
